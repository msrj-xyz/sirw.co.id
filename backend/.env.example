# PostgreSQL connection (fill with your dev server values)
# You can either set DATABASE_URL directly or set individual parts below and use them to build the URL.
POSTGRES_HOST=your-postgres-host.example.com
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=sirw_dev

# Example explicit DATABASE_URL (uncomment and edit if you prefer a single value)
# DATABASE_URL="postgresql://<user>:<password>@<host>:<port>/<db>?schema=public"

# Redis connection (choose one of the options below)
# 1) Simple host/port (no auth)
REDIS_HOST=your-redis-host.example.com
REDIS_PORT=6379
# 2) If Redis requires authentication set REDIS_PASSWORD or provide full REDIS_URL with credentials
REDIS_PASSWORD=
# Example full URL with password (preferred):
# REDIS_URL="redis://:your_redis_password@your-redis-host.example.com:6379"

# Convenience (if set, application will prefer DATABASE_URL over the parts)
DATABASE_URL=
REDIS_URL=

# App
PORT=4000

# JWT (RS256) - during dev you can generate a keypair and point to files here
JWT_PRIVATE_KEY_PATH=./dev-keys/private.key
JWT_PUBLIC_KEY_PATH=./dev-keys/public.key

# Storage (GCS) - used in production
GCS_BUCKET=
GCS_KEYFILE=

# SECURITY NOTE:
# - Do NOT commit a populated .env with secrets to source control.
# - Use your CI/CD / secret manager to inject production secrets.
